name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'
      - run: npm ci
      - run: |
          sed -i "s/version: '.*'/version: '1.${{ github.run_number }}.${{ github.run_attempt }}'/" src/environments/environment.ts
          sed -i "s/sha: '.*'/sha: '${{ github.sha }}'/" src/environments/environment.ts
      - run: npm run build -- --configuration production --base-href "/${{ github.event.repository.name }}/"
      - uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist/${{ github.event.repository.name }}/browser